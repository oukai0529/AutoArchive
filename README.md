# AutoArchive (IC Engineer Edition)

> 一个基于 Python + 7-Zip + GitHub Gist 的自动化冷备份与归档工具。
> 专为解决网盘限速、资源审查与本地密码管理痛点而设计。

## 📖 项目简介

**AutoArchive** 是一个轻量级的桌面自动化工具，旨在实现“无感加密”与“冷数据归档”。

它解决了两个核心问题：
1.  **隐私与安全**：本地文件通过 AES-256 强加密打包，文件名混淆，防止网盘审查。
2.  **密码管理焦虑**：利用文件指纹（MD5）将解压密码自动同步至你的 GitHub Gist 私有库。即使本地数据丢失，只要文件还在，就能自动找回密码。

## ✨ 核心功能

* **🖱️ 拖拽交互**：极简操作，拖入文件夹即打包，拖入压缩包即解压。
* **🔒 自动加密**：调用 7-Zip 内核进行 AES-256 加密，自动生成高强度随机密码。
* **☁️ 云端密钥库**：基于 GitHub Gist 的 Serverless 架构，自动同步文件指纹与密码，无需自建服务器。
* **⚡ 极速传输**：自动将大量碎文件打包成单一大文件（Store 模式），跑满网盘上传带宽。
* **🛡️ 配置分离**：支持 `config.json` 配置文件，敏感信息（Token）与代码完全分离，安全可靠。

## 🖼️ 软件截图

![主界面运行截图](screenshots/demo.png)
*(注：请在你的项目中创建一个 screenshots 文件夹，并将你的运行截图放入其中命名为 demo.png)*

## 🚀 快速开始

### 方式一：直接使用 (Windows)
1.  前往 [Releases](https://github.com/你的GitHub用户名/AutoArchive/releases) 页面下载最新的 `gui.exe`。
2.  确保电脑已安装 [7-Zip](https://www.7-zip.org/) (默认路径 `C:\Program Files\7-Zip\7z.exe`)。
3.  双击运行，根据弹窗提示输入你的 GitHub Token 和 Gist ID 即可。

### 方式二：源码运行
如果你想自己修改代码或在非 Windows 环境尝试：

1.  **克隆仓库**
    ```bash
    git clone [https://github.com/你的GitHub用户名/AutoArchive.git](https://github.com/你的GitHub用户名/AutoArchive.git)
    cd AutoArchive
    ```

2.  **安装依赖**
    ```bash
    pip install -r requirements.txt
    ```

3.  **运行**
    ```bash
    python gui.py
    ```

## ⚙️ 配置说明 (首次运行)

首次运行时，软件会自动检测并弹窗要求输入以下信息：

* **GitHub Token**: 用于读写 Gist。请在 GitHub Settings -> Developer settings -> Personal access tokens 中生成，**必须勾选 `gist` 权限**。
* **Gist ID**: 你的云端数据库 ID。请先在 [gist.github.com](https://gist.github.com/) 创建一个空的 Gist，复制 URL 末尾的字符串填入。

## 🛠️ 技术栈

* **语言**: Python 3.12+
* **GUI**: Tkinter + TkinterDnD2 (支持拖拽)
* **核心**: 7-Zip (命令行调用)
* **云同步**: GitHub REST API

## 📄 开源协议

MIT License © 2026 Wang Jiawei